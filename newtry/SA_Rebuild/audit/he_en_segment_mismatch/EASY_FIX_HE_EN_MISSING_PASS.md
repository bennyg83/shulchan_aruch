# Easy-fix pass: he_missing / en_missing

Date: **2026-08-27T18:29Z** (LIVE repo)

Scope: easy same-cell restores only. No seif-remap / cross-siman hunting. No `en_has_more` / `en_trunc` rejoin. **No YD editorial TXT as EN source.**

## Part A — OC `he_missing` (59)

Command (dry-run first):

```text
node restore_he_from_sefaria_pulls.mjs --dry-run --volume oc1 --kinds he_missing
```

Result:

| Metric | Count |
|--------|------:|
| mismatched `he_missing` | 59 |
| **restored / fixable** | **0** |
| **skipped** | **59** |

Skipped reason (all 59): same-cell Sefaria Pulls HE is empty/`null` across every candidate source the script checks (`sef_he_html`, `sef_json_layer`, `editorial_txt`, `merged_aligned`, `sibling_en_similar`). Spot-checks of `Sefaria Pulls/.../Orach_Chayim/simanim/{001,135,137,363}/seif-*/…/he.html` are length≈2 (blank). These are orphan EN placements (wrong-seif / no mechaber hook), not HE-dropouts with recoverable same-cell Hebrew.

**Apply not run** — dry-run fixable=0; no LIVE corpus `he.html` writes.

Clusters still open: siman **1** (5), **135** (27), **137** (26), **363** (1).

Artifacts:

- `he_restore_dry_run.json` (at 2026-08-27T18:25:57.908Z)
- `SEFARIA_HE_RESTORE_MAP.md` (regenerated by dry-run)

## Part B — YD `en_missing` (20)

Paths: `mateh-yehonatan` (8), `yad-avraham` (8), `rabbi-akiva-eiger-yd` (4) — see `MISSING_HE_EN_LISTS.md`.

| Source checked | Result |
|----------------|--------|
| LIVE corpus `en.html` at listed paths | **Missing file** on all 20 (HE present, EN absent) |
| Copy2 corpus same paths | **Absent** (no donor EN) |
| Other LIVE yd1 cells for same slugs | Many nonempty EN elsewhere (mateh/yad/rae exist on other simanim) — **not** matching keys for these 20 |
| Cleaned EN JSON/bundle in repo | **None found** for these keys |
| Sefaria Pulls YD commentaries | HE `merged.json` only (Mateh / Yad Avraham / RAE) — **no EN** |
| `YD_001/output/**/*.txt` | Has `**** ENGLISH ****` markers (e.g. siman 121 mateh) — **forbidden** per user rule (old MT risk) |

| Metric | Count |
|--------|------:|
| **applied** | **0** |
| **skipped** | **20** (no safe non-TXT EN) |

## Part C — Validate / recount

Focused recount of list paths (`recount_missing_lists.mjs` → `EASY_FIX_RECOUNT.json`):

- `he_missing` still **59 / 59**
- `en_missing` still **20 / 20**

Prior full-volume scan (`ALL_volumes.json`, scannedAt 2026-08-27T17:19:54.378Z) already matched these counts; corpus HTML unchanged this pass.

## Git

Corpus HTML/JSON under `public/corpus/` **not modified**. Commit covers audit notes / dry-run logs only (if committed).
