# OC001 English — house style (short)

- Use the workspace **full_dictionary** pass on every English edit (`npm run apply:dictionary` from `newtry/OC_001`). That file at the repo root is authoritative for terminology normalization.
- Transliterate Hebrew names and common rabbinic terms consistently with existing OC001 slices in the same siman when in doubt.
- Keep **legal-technical** tone: clear, dignified English; avoid colloquial paraphrase of halachic conclusions.
- Preserve inline HTML that mirrors Sefaria hooks (`<i data-commentator="…">`, `<small>` for Hagah, `<b>` for headings).
- Do not alter `**** HEBREW ****` sections or block header lines (`slug:`, `seif:`, `marker:`) unless a tooling bug requires a fix.
