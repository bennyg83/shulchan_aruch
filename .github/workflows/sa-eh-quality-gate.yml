name: EH quality gate

on:
  pull_request:
    branches: [eh-development, "eh/**"]
    paths:
      - "newtry/EH_001/**"
      - "full_dictionary (1).md"
      - ".github/workflows/sa-eh-quality-gate.yml"
  push:
    branches: [eh-development]
    paths:
      - "newtry/EH_001/**"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    defaults:
      run:
        working-directory: newtry/EH_001
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Structure check
        run: npm run pipeline:structure-check

      - name: Quality validation
        run: npm run pipeline:validate:quality
        continue-on-error: true

      - name: Upload quality report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eh-quality-report
          path: newtry/EH_001/checklist-output/
          if-no-files-found: ignore
